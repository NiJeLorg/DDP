import React from "react";

class MobileNavStoryMap extends React.Component {
    constructor(props) {
        super(props);
    }

    // render() {
    //     return (
    //         <div className={"c-mobile-nav-story-map"}>
    //           <button className={`nav-story-button__${ (this.props.view === 'story') ? 'active' : 'inactive' }`} name="story" onClick={this.props.handleViewSelect}>Story View</button>
    //           <button className={`nav-story-button__${ (this.props.view === 'map') ? 'active' : 'inactive' }`} name="map" onClick={this.props.handleViewSelect}>Map View</button>
    //         </div>
    //       );
    // }
};

export default MobileNavStoryMap;
